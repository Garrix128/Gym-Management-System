# 体育训练管理系统 — 产品需求文档 (PRD)

> **版本:** v0.1-alpha  
> **状态:** 草案 — 待持续完善  
> **设计系统:** Nike Design System (DESIGN.md)  
> **原型版本:** prototype.html (3 变体: A-仪表盘 / B-编辑风格 / C-看板)  
> **最后更新:** 2026-05-10

---

## 问题描述

中小型健身房、体育训练馆和私教工作室在日常运营中面临以下痛点:

1. **信息分散** — 会员档案、课程排期、教练调度分别存放在 Excel、纸质表格和微信聊天中,缺乏统一入口。管理者无法在一个页面内看到"今天的排课 + 本周新注册会员"。

2. **排课效率低下** — 教练和课程的时间安排依赖人工协调，满员/冲突/空缺无法实时感知。当一门团课满员时，管理者无法快速看到候补名单并做出调整。

3. **会员管理粗放** — 会员的体测数据、出勤记录状态分散在多个文档中。教练无法在开课前快速了解一个会员的训练历史和当前体能水平。

4. **缺乏运营洞察** — 管理者没有 KPI 仪表盘（活跃会员数、课程数量、出勤率），决策依赖直觉而非数据。

**目标用户:** 健身房/训练馆的运营管理者、前台工作人员、教练团队负责人。

---

## 解决方案

构建一个 **体育训练管理系统 (Gym Training Management System)**，为健身房和训练馆提供一站式的数字化运营平台。系统采用 **Nike 设计系统**的美学标准 — 极致排版对比、纯黑白灰配色、药丸形 CTA、摄影优先的编辑风格，在专业感与运动感之间取得平衡。

核心交付物:
- **仪表盘 (Dashboard):** KPI 卡片、动态时间线、日历、今日课程 — 一站式运营总览
- **会员管理 (Members):** 会员档案、体测记录、出勤追踪
- **课程管理 (Classes):** 团课/私教课创建、排期、满员/候补状态管理
- **看板视图 (Kanban):** 训练任务从"待排课"到"已完成"的全流程追踪
- **教练管理 (Trainers):** 教练档案、资质、评分、学员统计

---

## 用户故事

### 仪表盘

1. 作为健身房管理者，我想要一打开系统就看到今日的核心运营指标（活跃会员、本月课程、在岗教练），以便快速了解场馆运营状况。
2. 作为管理者，我想要在仪表盘上看到实时的最近动态（新会员注册、课程满员、体测完成），以便及时关注重要事件。
3. 作为管理者，我想要一个日历视图显示本月的课程安排概览，以便快速定位哪天课程较多或较少。
4. 作为管理者，我想要看到今日课程列表（时间、课程名、教练、是否满员），以便了解当天运营节奏。
5. 作为管理者，我想要仪表盘展示会员出勤率、教练平均评分两项关键质量指标，以便评估服务质量。

### 会员管理

6. 作为前台工作人员，我想要能够快速搜索和查看会员档案（姓名、会员计划类型、入会日期、出勤率），以便在会员到店时高效服务。
7. 作为教练，我想要查看会员的体测历史记录（InBody 体测、体脂率变化趋势），以便为私教课制定个性化训练计划。
8. 作为管理者，我想要一键新增会员并选择会员计划（基础/Pro/私教），以便简化入会流程。
9. 作为管理者，我想要看到"本月之星"等会员荣誉展示（出勤率、进步最快等徽章），以便激励会员并营造社区氛围。
10. 作为管理者，我想要在会员即将到期时收到提醒，以便主动联系会员。

### 课程管理

11. 作为管理者，我想要创建团课和私教课并设置时间、教练、最大容量和价格，以便标准化课程体系。
12. 作为管理者，我想要在课程详情中实时看到已报名人数和剩余席位，以便判断是否需要加开课程或开启候补。
13. 作为会员（系统代理），当我尝试报名已满员课程时，我想要能够登记候补，以便在有人退出时自动获得名额。
14. 作为教练，我想要查看自己本周/本月的排课时间表，以便合理安排个人时间。
15. 作为管理者，我想要按教练、按课程类型筛选课程列表，以便快速定位特定课程。

### 看板工作流

16. 作为管理者，我想要将训练任务、课程从"待排课"拖拽到"进行中"再到"已完成"的看板视图，以便直观管理课程生命周期。
17. 作为管理者，我想要在看板卡片上看到任务类型（团课/私教/营养咨询）、教练分配和时间安排，以便快速了解任务概况。
18. 作为管理者，我想要在看板中一目了然各列（待排课/进行中/满员候补/已完成）的任务数量，以便评估团队工作负载。

### 教练管理

19. 作为管理者，我想要查看教练团队列表（姓名、专业领域、经验年限、评分、累计学员数），以便评估教练团队整体水平。
20. 作为管理者，我想要查看每个教练的评分和学员出勤率数据，以便发现优秀教练和改进空间。
21. 作为教练，我想要维护自己的个人资料和专业领域标签（如"力量训练 · 运动康复"），以便会员找到合适的教练。

### 系统体验

22. 作为用户，我应该能够在纯键盘操作下完成主要导航（Tab 切换、Enter 确认），以便满足无障碍访问标准。
23. 作为用户，我应该看到一致的 Nike 设计风格（药丸按钮、极致排版对比、无阴影扁平卡片），以便获得专业且运动的视觉体验。
24. 作为用户，系统在移动端应该自适应布局（侧边栏收起到汉堡菜单、KPI 网格从 3 列降为 2 列/1 列），以便在手机上也能完成日常操作。

---

## 实施决策

### 1. 技术架构

| 层级 | 选型 | 理由 |
|------|------|------|
| 前端框架 | React 18+ (可选 Next.js) | 生态成熟，组件化开发高效；Next.js 提供 SSR 和文件路由 |
| 样式方案 | TailwindCSS + CSS Variables (DESIGN.md tokens) | Tailwind 与设计 Token 可直接映射，配合 `@apply` 封装组件样式 |
| UI 组件 | 自研组件库（基于 DESIGN.md 组件规范） | Nike 设计系统独特（药丸形 CTA、0 阴影卡片），现有 UI 库无法直接匹配 |
| 状态管理 | React Context + useReducer（初期）/ Zustand（扩展） | 初期功能模块少，Context 足够；扩展时可按需引入轻量状态库 |
| 图标系统 | 内联 SVG（18-20px viewBox，`currentColor` 继承） | 零依赖、可 CSS 控制、跨平台一致 |
| 路由 | React Router v6 (CSR) 或 Next.js App Router (SSR) | 取决于是否采用 Next.js |
| 字体 | Inter (UI) + Bebas Neue (Display) — Google Fonts 加载 | Nike Futura ND 为商业字体，Bebas Neue 为最接近的开源替代 |

### 2. 模块拆分

| 模块 | 职责 | 深度模块？ | 说明 |
|------|------|-----------|------|
| **DesignTokens** | 提供全局 CSS 变量（颜色、字体、间距、圆角）和 Tailwind 主题映射 | 浅层 | 纯配置模块，无逻辑 |
| **BaseUI** | 封装按钮 (Primary/Secondary/Outline)，搜索框 (SearchPill)，徽章 (Badge)，芯片 (Chip)，卡片 (Card) | 深层 | 封装 Nike 设计系统的所有基础组件，提供简洁的 props 接口 (variant, size, active 等) |
| **Dashboard** | KPI 卡片、动态时间线、日历、今日课程 | 浅层 | 组合 BaseUI 组件 + 数据获取 |
| **MemberModule** | 会员 CRUD、体测记录、出勤图表、会员列表/搜索/筛选 | 常规 | 表单密集型 + 列表展示 |
| **ClassModule** | 课程 CRUD、排期、报名/候补、教练分配 | 常规 | 涉及状态机（待排课→进行中→已完成） |
| **KanbanModule** | 看板列/卡片、拖拽排序（可选）、任务状态流转 | 深层 | 封装通用的看板逻辑，可用于课程排期和后续其他流程 |
| **TrainerModule** | 教练档案、评分系统、学员统计 | 常规 | 相对简单的 CRUD |
| **Router** | 页面路由和导航守卫 | 浅层 | 框架提供 |

### 3. 数据模型

**会员 (Member)**
```
id, name, phone, email, avatar, plan_type (Basic/Pro/PT),
join_date, expiry_date, attendance_rate,
bodyscale_records: [{date, body_fat_pct, muscle_mass, ...}],
badges: [{type: "star"|"improvement"|"full_attendance"|"bodyscale_pass"}]
```

**课程 (Class)**
```
id, name, type (group/pt/nutrition), description,
trainer_id, schedule (weekly重复模式 或 single日期),
start_time, end_time, max_capacity, price,
status (draft/scheduled/in_progress/full/waitlist/completed)
enrolled_members: [member_id],
waitlist: [member_id]
```

**教练 (Trainer)**
```
id, name, specialties: [string], experience_years,
rating, total_students, avatar
```

**看板任务 (KanbanTask)**
```
id, class_id (optional), type, title, description,
status (pending/in_progress/full_waitlist/completed),
trainer_id, schedule, created_at, updated_at
```

### 4. 导航结构

```
/ (重定向到 /dashboard)
/dashboard          — 仪表盘
/members            — 会员列表
/members/:id        — 会员详情（含体测记录）
/members/new        — 新增会员
/classes            — 课程列表
/classes/:id        — 课程详情
/classes/new        — 新建课程
/kanban             — 看板工作流
/trainers           — 教练列表
/trainers/:id       — 教练详情
/settings           — 系统设置
```

### 5. 设计系统约束

以下约束来自 DESIGN.md，所有开发必须遵守:

- **CTA 形状:** 仅限于 `rounded.lg` (30px) 药丸形 — 绝不允许方形或小圆角按钮。
- **色彩纪律:** 95% 的 UI 面积仅用 `#111111`、`#ffffff`、`#f5f5f5` 三种色。`#d30005` (红) 仅用于价格/满员信号，`#007d48` (绿) 仅用于成功/出勤信号。
- **无投影:** 卡片和按钮不使用 box-shadow。唯一的深度线索是 sticky bars 底部的 1px inset hairline。
- **字体层级:** `display-campaign` (96px Bebas Neue, 大写) 仅用于编辑性主视觉，绝不用于功能标题。功能标题使用 `heading-xl` (32px)。
- **图标:** 内联 SVG、`currentColor`、18px viewBox，不使用 emoji 作为功能图标。

### 6. UI 变体策略（来自原型结论）

原型提供了三种结构不同的 Dashboard 设计方案:

| 变体 | 设计思路 | 适用场景 | 采纳建议 |
|------|----------|----------|----------|
| **A: 仪表盘指挥中心** | 侧边栏 + KPI 网格 + 动态时间线 + 日历 | 运营管理者日常使用 | ✅ 核心方案 — 作为默认 Dashboard |
| **B: 运动编辑风格** | Nike 风全幅 Hero + 横向滚动课程 + 教练网格 + 会员列表 | 对外展示/营销页面、教练端首页 | ✅ 作为营销落地页和教练端首页 |
| **C: 看板工作流** | 四列看板追踪课程生命周期 | 课程/排课管理深度操作 | ✅ 作为独立的课程排期管理视图 |

**最终采纳:** 三种变体各司其职 — A 为默认运营仪表盘，B 为对外/教练端展示，C 为课程排期专用工作流。非"三选一"而是"三者共存"。

---

## 测试决策

### 测试原则

- **只测试外部行为，不测试实现细节** — 例如测试"点击立即报名按钮后，course-card 显示'候补登记'"而不测试内部 state 变量名。
- **优先测试深层模块** — `BaseUI`（按钮、搜索框、徽章）和 `KanbanModule`（状态流转逻辑）是最值得测试的模块，因其接口稳定、调用频率高、边界情况丰富。
- **业务逻辑优先于 UI 快照** — 避免对组件 DOM 做快照测试；关注业务规则（满员→候补、过期→提醒）的单元测试。
- **不使用 mock 测试数据获取** — 测试数据层时使用内存数据库或 fixture 文件。

### 需测试的模块

| 模块 | 测试类型 | 测试重点 |
|------|----------|----------|
| **BaseUI** | 单元测试 (Vitest + Testing Library) | 按钮的 variant/size/disabled 状态正确渲染和触发 onClick；搜索框的 focus/blur 样式切换；芯片的 active 切换 |
| **KanbanModule** | 单元测试 | 卡片状态从 pending→in_progress→completed 的流转规则；列内卡片排序；满员检测逻辑 |
| **ClassModule** | 集成测试 | 课程创建→报名→满员→候补→完成的完整流程；跨教练排课时间冲突检测 |
| **MemberModule** | 集成测试 | 会员新增→体测记录→出勤更新的数据一致性 |
| **Dashboard** | 基本渲染测试 | KPI 卡片数据绑定正确性；动态时间线按时间排序 |

### 不测试的内容

- 第三方库（React Router、Google Fonts）— 由库自身保证
- CSS 视觉回归 — 初期不做，等设计定型后再考虑 Percy/Chromatic
- 日历组件日期计算 — 依赖 `date-fns` 库，仅测试业务层对日期的使用

---

## 范围外（Out of Scope）

以下功能明确不在当前版本范围内，将在后续版本迭代:

1. **在线支付集成** — 不在当前版本范围，不接入微信支付/支付宝。
2. **会员端 App/小程序** — 当前版本仅包含管理后台 Web 端。会员自主预约、查看课表的客户端不在范围内。
3. **实时消息通知** — 不会在 MVP 中实现短信/推送通知（课程提醒等）。可在系统内做站内标记。
4. **多门店/连锁管理** — 当前仅支持单一场馆的数据管理。
5. **数据导出/报表生成** — 运营报表的 PDF/Excel 导出不在当前版本。
6. **教练排班日历** — 教练个人可用时间管理（非上课时间）的排班功能不在当前版本。
7. **门禁/签到硬件集成** — 不与健身房闸机、指纹打卡设备对接。
8. **国际化 (i18n)** — 当前仅支持简体中文。
9. **视频课程/在线训练** — 纯线下训练管理，不涉及线上教学。
10. **营销自动化** — 不包含邮件营销、优惠券系统、裂变活动等。

---

## 补充说明

1. **本 PRD 为草案版本**，将随着开发推进持续细化。每个功能模块在开始开发前应产出更详细的技术规格文档。
2. **DESIGN.md** 是本项目的设计宪法，所有 UI 决策必须与其对齐。任何偏离设计系统的实现必须在 ADR（架构决策记录）中说明理由。
3. **原型文件 `prototype.html`** 是纯静态 HTML，用于验证设计方向。其代码不应直接迁移到生产环境，而应基于 PRD 中的模块拆分重新架构。
4. **ui-ux-pro-max 技能** 定义了 UI/UX 优化的具体规则（图标系统、视觉层级、交互细节），开发过程中应持续遵循。
5. **"ui-ux-pro-max" 技能不应被 UI 组件库中的 emoji 图标所替代**，使用 SVG 内联图标系统，尽量采用 18-20px viewBox 和 `currentColor` 继承颜色。
